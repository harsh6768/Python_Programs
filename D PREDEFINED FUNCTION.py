"""

1. cmp(dic1,dic2)---compares elements of both dictionary
2. len(dict)---Gives the total length of the dictionary. This would be equal
                to the number of items in the dictionary.
3. str(dict)-- Produces a printable string representation of a dictionary
4 type(variable)---Returns the type of the passed variable. If passed variable is dictionary,
                   then it would return a dictionary type.

METHODS :::
1 dict.clear()----Removes all elements of dictionary dict
2.dict.copy()----Returns a shallow copy of dictionary dict
3.dict.fromkeys()---Create a new dictionary with keys from
4. dict.get(key,default=none)
5.dict.setdafault(key,default=none)
6.dict.keys()------- Returns a list of dict's keys
7. dict.items()-----Returns a lists of dict's (key,value) tuple pairs
8.dict.update(dict2)-----------
9.dict.values()----Returns list of dictinary dicr's values


"""